# 贡献指南

感谢你参与 dsh-cost-meter。本文说明本仓库的开发、评审和发布前检查要求。

## 开发环境

- 使用 Node.js `^22.19.0 || >=24.0.0`，npm 以仓库锁文件为准。
- 按锁文件安装依赖：

```sh
npm ci
```

- 常用命令：
  - `npm test`：运行单元测试。
  - `npm run typecheck`：运行 TypeScript 类型检查。
  - `npm run build`：执行类型检查并构建插件。
  - `npm run verify`：运行测试、构建和包校验。
  - `npm run dev`：启动本地视觉演示。

## 分支与 PR

- 从最新主干创建短生命周期分支，分支名应描述变更范围，例如 `fix/price-rounding` 或 `docs/contributing`.
- PR 应保持聚焦，避免混入无关格式化、重命名或依赖更新。
- PR 描述需说明变更目的、验证命令和对价格计算、账本、插件打包或 UI 的影响。
- 行为变更应包含相应测试；确实无法自动化测试时，在 PR 中说明人工验证步骤。

## 测试门禁

提交前至少运行：

```sh
npm run verify
```

如果变更只涉及文档或 GitHub 模板，可说明未运行完整验证的原因。涉及 TypeScript、构建产物、插件入口、价格计算、账本写入或包发布元数据的变更必须运行相关自动化检查。

## 价格目录变更要求

价格目录会直接影响成本计算结果。任何价格变更都必须：

- 标明价格来源、发布日期或生效日期。
- 说明币种、计费单位、输入/输出 token 口径和缓存/折扣规则。
- 覆盖新增、修改或废弃的模型别名。
- 增加或更新测试，覆盖边界值、缺省价格和回退行为。
- 在 PR 中说明对历史账本和用户可见成本展示的影响。

## 隐私与安全红线

- 不要提交 API key、访问令牌、Cookie、会话、私钥、内部 URL 或任何真实用户数据。
- 日志、截图和测试夹具必须脱敏，至少移除账号标识、请求 ID、会话 ID、token、文件路径中的用户名和业务敏感内容。
- 不要扩大本地账本、遥测或插件运行时收集的数据范围，除非 PR 明确说明目的、保留策略和用户控制方式。
- 安全漏洞不要提交到公开 issue；请按 `SECURITY.md` 的说明报告。

## 提交说明

- 提交信息使用简短祈使句，说明实际变更，例如 `Add MIT license metadata`.
- 将不相关变更拆成独立提交，方便评审和回滚。
- 依赖或锁文件变更应说明触发原因；只更新锁文件时请避免改动无关包版本。
